---
import { COPY, type Lang } from '../content/copy';
const { lang } = Astro.props as { lang: Lang };
const t = COPY[lang].tesis;
---
<section id="tesis" class="py-24 md:py-32 border-t border-rule">
  <div class="mx-auto max-w-6xl px-6 lg:px-10">
    <p class="text-eyebrow uppercase tracking-eyebrow text-mute mb-5">{t.kicker}</p>
    <h2 class="font-display text-display-2 leading-display tracking-tight max-w-3xl">{t.title}</h2>
    <div class="mt-14 grid md:grid-cols-3 gap-10 md:gap-12">
      {t.items.map((it, i) => (
        <div>
          <div class="font-display text-caption leading-none mb-6 tracking-eyebrow uppercase"><span class="text-accent font-semibold">0{i + 1}</span> <span class="text-mute">—</span></div>
          <h3 class="font-display text-h3 leading-snug tracking-tight mb-4">{it.h}</h3>
          <p class="text-body leading-relaxed text-ink/75">{it.p}</p>
        </div>
      ))}
    </div>
  </div>
</section>
